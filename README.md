
  # GOAL PLANNER

  This is a code bundle for GOAL PLANNER. The original project is available at https://www.figma.com/design/2y7FuiqUCddNs4MSTEPb2c/GOAL-PLANNER.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  